# ========================================
# Webhook Manager - Environment Variables
# ========================================
# 
# 复制此文件为 .env 并根据需要修改配置
# cp env.example .env
#
# ========================================

# ----------------------------------------
# Database Configuration
# ----------------------------------------
DB_USERNAME=webhook
DB_PASSWORD=webhook123
DB_DATABASE=webhook_manager
# 如果需要从宿主机访问数据库，可设置暴露端口
DB_PORT_EXPOSE=5432

# ----------------------------------------
# JWT Configuration
# ----------------------------------------
# ⚠️ 生产环境请务必修改此密钥！
JWT_SECRET=change-this-in-production-please
JWT_EXPIRES_IN=7d

# ----------------------------------------
# Security
# ----------------------------------------
BCRYPT_SALT_ROUNDS=10

# ----------------------------------------
# Database Auto Sync
# ----------------------------------------
# 首次部署设为 true，让 TypeORM 自动创建表
# 表结构创建完成后建议改为 false
# 默认: 开发环境 true，生产环境 false
DB_SYNCHRONIZE=true
# 生产环境需要设置 DB_SYNC_FORCE=true 才能启用 DB_SYNCHRONIZE
# 首次部署后建议将两者都改为 false
DB_SYNC_FORCE=true

# ----------------------------------------
# CORS Configuration
# ----------------------------------------
# 允许的跨域来源，多个用逗号分隔，* 表示允许所有
CORS_ORIGIN=*

# ----------------------------------------
# Port Configuration
# ----------------------------------------
# 后端 API 端口（内部通信用 3000，此为宿主机暴露端口）
BACKEND_PORT_EXPOSE=3000

# 前端访问端口
FRONTEND_PORT=80

# ----------------------------------------
# HTTPS Configuration (for docker-compose.https.yml / docker-compose.backend.yml)
# ----------------------------------------
# 你的域名（必须已解析到服务器 IP）
DOMAIN=your-domain.com

# Let's Encrypt 邮箱（用于证书过期通知）
ACME_EMAIL=your-email@example.com

# Traefik Dashboard 认证（可选，格式: user:password）
# 生成方法: htpasswd -nb admin your-password
TRAEFIK_DASHBOARD_AUTH=

# ----------------------------------------
# Standalone Frontend Configuration
# ----------------------------------------
# 前端域名（用于 docker-compose.frontend-https.yml）
FRONTEND_DOMAIN=www.example.com

# 后端 API 地址（前端独立部署时必须配置）
# 示例: https://api.example.com 或 https://your-domain.com
BACKEND_API_URL=https://api.example.com
